services:
  # FastAPIアプリケーションサービス
  api:
    build: . # カレントディレクトリのDockerfileを使用してイメージをビルド
    ports:
      - "8000:8000" # ホストの8000ポートをコンテナの8000ポートにマッピング
    volumes:
      - .:/src # ホストのカレントディレクトリをコンテナの/srcにマウント（ホットリロード有効）
